subjects:
  by_id:
    "Doctor_1": 
      scoring: u_curve
      params: { optimal_distance: 2.0, curve_width: 0.8, tolerance: 0.5 }
      average_weight: 5
    "Visitor_0": 
      scoring: distance_penalty
      params: { safe_distance: 1.0, penalty_weight: 1.0, tolerance: 0.5 }
      average_weight: 1
    "Visitor_1": 
      scoring: distance_penalty
      params: { safe_distance: 1.0, penalty_weight: 1.0, tolerance: 0.5 }
      average_weight: 1
    "Visitor_2": 
      scoring: distance_penalty
      params: { safe_distance: 1.0, penalty_weight: 1.0, tolerance: 0.5 }
      average_weight: 1
    "Visitor_3": 
      scoring: distance_penalty
      params: { safe_distance: 1.0, penalty_weight: 1.0, tolerance: 0.5 }
      average_weight: 1
    "Nurse_1": 
      scoring: distance_penalty
      params: { safe_distance: 1.5, penalty_weight: 1.5, tolerance: 0.5 }
      average_weight: 1
    "Nurse_2": 
      scoring: distance_penalty
      params: { safe_distance: 1.5, penalty_weight: 1.5, tolerance: 0.5 }
      average_weight: 1
    "Cleaner_1": 
      scoring: distance_penalty
      params: { safe_distance: 1.0, penalty_weight: 1.0, tolerance: 0.5 }
      average_weight: 1

zones:
  Patient Room 1:
    category: "patient"
    penalty_type: "forbidden_zone_violation"
    scoring: "zone_penalty"
    params:
      violation_penalty: -1.0          # Severe penalty for entering the patient room
      safe_distance: 1.0               # Safety distance from the patient room boundary
      penalty_weight: 3.0              # Penalty weight
      continuous_violation: true       # Whether continuous violations accumulate penalties
      time_multiplier: 1.0             # Time-based penalty multiplier (penalty per time step)
      max_penalty: -100.0               # Maximum penalty value

  Patient Room 2:
    category: "patient"
    penalty_type: "forbidden_zone_violation"
    scoring: "zone_penalty"
    params:
      violation_penalty: -1.0          # Severe penalty for entering the patient room
      safe_distance: 1.0               # Safety distance from the patient room boundary
      penalty_weight: 3.0              # Penalty weight
      continuous_violation: true       # Whether continuous violations accumulate penalties
      time_multiplier: 1.0             # Time-based penalty multiplier (penalty per time step)
      max_penalty: -100.0               # Maximum penalty value

  Patient Room 3:
    category: "patient"
    penalty_type: "forbidden_zone_violation"
    scoring: "zone_penalty"
    params:
      violation_penalty: -1.0          # Severe penalty for entering the patient room
      safe_distance: 1.0               # Safety distance from the patient room boundary
      penalty_weight: 3.0              # Penalty weight
      continuous_violation: true       # Whether continuous violations accumulate penalties
      time_multiplier: 1.0            # Time-based penalty multiplier (penalty per time step)
      max_penalty: -100.0               # Maximum penalty value

  Patient Room 4:
    category: "patient"
    penalty_type: "forbidden_zone_violation"
    scoring: "zone_penalty"
    params:
      violation_penalty: -1.0          # Severe penalty for entering the patient room
      safe_distance: 1.0               # Safety distance from the patient room boundary
      penalty_weight: 3.0              # Penalty weight
      continuous_violation: true       # Whether continuous violations accumulate penalties
      time_multiplier: 1.0             # Time-based penalty multiplier (penalty per time step)
      max_penalty: -100.0               # Maximum penalty value

  Patient Room 5:
    category: "patient"
    penalty_type: "forbidden_zone_violation"
    scoring: "zone_penalty"
    params:
      violation_penalty: -1.0          # Severe penalty for entering the patient room
      safe_distance: 1.0               # Safety distance from the patient room boundary
      penalty_weight: 3.0              # Penalty weight
      continuous_violation: true       # Whether continuous violations accumulate penalties
      time_multiplier: 1.0             # Time-based penalty multiplier (penalty per time step)
      max_penalty: -100.0               # Maximum penalty value

  scoring:
    normalize_by_episode_time: true